[package]
name = "ui"
version = "0.1.0"
edition = "2021"
description = "UI components for PWNGHOST-RS (display + web)"

[dependencies]
ui-display = { path = "display" }
ui-web = { path = "web" }

[features]
default = []
hardware = ["ui-display/hardware"]
