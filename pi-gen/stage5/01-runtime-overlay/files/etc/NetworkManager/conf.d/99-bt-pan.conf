# Prevent NetworkManager from managing Bluetooth PAN interfaces.
# bnep0 / bt-pan0 are brought up by bt-pan-connect via dhcpcd. If NM manages
# these too, it conflicts with dhcpcd and drops the BT tether.

[keyfile]
unmanaged-devices=interface-name:bnep*;interface-name:bt-pan*
